version: '3.8'

services:
  trading_bot:
    build: .
    container_name: ai_trading_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
